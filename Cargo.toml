[package]
name = "uncoauthor"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4", features = ["derive"] }
dialoguer = { version = "0.12.0", default-features = false, features = ["fuzzy-select"] }
